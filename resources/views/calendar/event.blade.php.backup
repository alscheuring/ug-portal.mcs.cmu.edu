<x-layouts.public :title="$event->title" :team="$team">
<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    {{-- Breadcrumb --}}
    <nav class="mb-6">
        <ol class="flex items-center space-x-2 text-sm text-gray-500">
            <li><a href="{{ route('public.team.index', $team->slug) }}" class="hover:text-gray-700">{{ $team->name }}</a></li>
            <li>/</li>
            <li><a href="{{ route('public.calendar.team', $team->slug) }}" class="hover:text-gray-700">Events</a></li>
            <li>/</li>
            <li class="text-gray-900">{{ $event->title }}</li>
        </ol>
    </nav>

    {{-- Event Header --}}
    <div class="mb-8">
        <div class="flex items-start justify-between">
            <div class="flex-1 min-w-0">
                <h1 class="text-3xl font-bold text-gray-900 mb-2">{{ $event->title }}</h1>

                @if($event->summary)
                    <p class="text-lg text-gray-600 mb-4">{!! strip_tags($event->summary) !!}</p>
                @endif

                {{-- Status Badges --}}
                <div class="flex flex-wrap items-center gap-2 mb-4">
                    @if($event->isHappening())
                        <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-green-100 text-green-800">
                            🟢 Happening Now
                        </span>
                    @elseif($event->isUpcoming())
                        <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-blue-100 text-blue-800">
                            🕐 Upcoming
                        </span>
                    @else
                        <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-gray-100 text-gray-800">
                            📁 Past Event
                        </span>
                    @endif

                    <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium {{ $event->source_type === 'imported' ? 'bg-gray-100 text-gray-800' : 'bg-blue-100 text-blue-800' }}">
                        {{ $event->source_type === 'imported' ? '📥 Imported Event' : '✏️ Manual Event' }}
                    </span>
                </div>
            </div>

            {{-- Actions --}}
            <div class="ml-4 flex items-center space-x-2">
                <a href="{{ route('public.calendar.team', $team->slug) }}" class="inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50">
                    ← Back to Calendar
                </a>
                @if($event->info_url)
                    <a href="{{ $event->info_url }}" target="_blank" class="inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700">
                        View Details ↗
                    </a>
                @endif
            </div>
        </div>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
        {{-- Main Content --}}
        <div class="lg:col-span-2 space-y-6">
            {{-- Event Description --}}
            @if($event->description)
                <div class="bg-white shadow rounded-lg">
                    <div class="p-6">
                        <h2 class="text-lg font-medium text-gray-900 mb-4">About This Event</h2>
                        <div class="prose prose-gray max-w-none">
                            {!! $event->description !!}
                        </div>
                    </div>
                </div>
            @endif

            {{-- Event Image --}}
            @if($event->image_url)
                <div class="bg-white shadow rounded-lg">
                    <div class="p-6">
                        <h2 class="text-lg font-medium text-gray-900 mb-4">Event Image</h2>
                        <img src="{{ $event->image_url }}" alt="{{ $event->title }}" class="w-full rounded-lg">
                    </div>
                </div>
            @endif
        </div>

        {{-- Sidebar --}}
        <div class="space-y-6">
            {{-- Event Details --}}
            <div class="bg-white shadow rounded-lg">
                <div class="p-6">
                    <h2 class="text-lg font-medium text-gray-900 mb-4">Event Details</h2>
                    <div class="space-y-4">
                        {{-- Date and Time --}}
                        <div>
                            <div class="font-medium text-gray-900 flex items-center mb-2">
                                📅 Date & Time
                            </div>
                            <div class="text-gray-600">
                                {{ $event->start_datetime->format('l, F j, Y') }}<br>
                                {{ $event->start_datetime->format('g:i A') }} - {{ $event->end_datetime->format('g:i A') }}
                            </div>
                            <div class="text-sm text-gray-500 mt-1">
                                Duration: {{ $event->formatted_duration }}
                            </div>
                        </div>

                        {{-- Location --}}
                        @if($event->location)
                            <div>
                                <div class="font-medium text-gray-900 flex items-center mb-2">
                                    📍 Location
                                </div>
                                <div class="text-gray-600">
                                    {{ $event->location }}
                                </div>
                            </div>
                        @endif

                        {{-- Author --}}
                        @if($event->author)
                            <div>
                                <div class="font-medium text-gray-900 flex items-center mb-2">
                                    👤 Created by
                                </div>
                                <div class="text-gray-600">
                                    {{ $event->author->name }}
                                </div>
                            </div>
                        @endif

                        {{-- Import Source --}}
                        @if($event->eventFeed)
                            <div>
                                <div class="font-medium text-gray-900 flex items-center mb-2">
                                    📡 Imported from
                                </div>
                                <div class="text-gray-600">
                                    {{ $event->eventFeed->name }}
                                </div>
                            </div>
                        @endif

                        {{-- Tags --}}
                        @if($event->tags)
                            <div>
                                <div class="font-medium text-gray-900 flex items-center mb-2">
                                    🏷️ Tags
                                </div>
                                <div class="flex flex-wrap gap-1">
                                    @foreach($event->tags as $tag)
                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-800">
                                            {{ $tag }}
                                        </span>
                                    @endforeach
                                </div>
                            </div>
                        @endif
                    </div>
                </div>
            </div>

            {{-- Quick Actions --}}
            <div class="bg-white shadow rounded-lg">
                <div class="p-6">
                    <h2 class="text-lg font-medium text-gray-900 mb-4">Quick Actions</h2>
                    <div class="space-y-3">
                        @if($event->info_url)
                            <a href="{{ $event->info_url }}" target="_blank" class="w-full inline-flex justify-center items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50">
                                🔗 View Full Details
                            </a>
                        @endif

                        <button disabled class="w-full inline-flex justify-center items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-400 bg-gray-50 cursor-not-allowed">
                            📅 Add to Calendar (Coming Soon)
                        </button>

                        <button disabled class="w-full inline-flex justify-center items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-400 bg-gray-50 cursor-not-allowed">
                            📤 Share Event (Coming Soon)
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
</x-layouts.public>